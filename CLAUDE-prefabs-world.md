<!-- Companion to CLAUDE.md. Read when working on doors, structures, lockers, elevators, glass, targets, admin toys, or UI element prefabs. -->
# CLAUDE-prefabs-world.md — World / Structure / UI Prefab Reference

Generated by parsing actual `.prefab` YAML (Session 8). All in `Assets/GameObject/` unless noted.

---

## Doors

| Prefab | GOs | Scripts |
|---|---|---|
| `LCZ BreakableDoor` | 18 | `BreakableDoor`, `DoorEventOpenerExtension`, `DoorLinkingRooms`, `InteractableCollider`, `NetIdWaypoint`, NetworkIdentity, `RegularDoorButton`, Animator, AudioSource |
| `HCZ BreakableDoor` | 13 | same set |
| `EZ BreakableDoor` | 20 | same set + nametag UI |
| `LCZ BrokenDoor` | 9 | `BrokenDoor` (post-destruction static) |
| `HCZ BrokenDoor` | 5 | `BrokenDoor` |
| `EZ BrokenDoor` | 7 | `BrokenDoor` |
| `Prison BrokenDoor` | 4 | `BrokenDoor` |

Doors are spawned at the 343 `DoorSpawnpoint`s in Facility after map generation (`DoorSpawnpoint.SetupAllDoors()`, server-only). Checkpoint/pryable/elevator doors are scene objects or part of room hierarchies, not standalone prefabs here. `Checkpoint_Breakable` (161 GOs, `SpawnableRipple`) = breakable checkpoint glass wall; breakable glass: `049_BreakableGlass`, `lcz_gr_BreakableGlass`, `lcz_plants_BreakableGlass`.

## Elevators

- `ElevatorChamber` / `ElevatorChamber Gates` (19 GOs): `ElevatorChamber`, `ElevatorPanel`, `ElevatorWaypoint`, `DynamicCullableBase`, `FlickerableLight`. Spawned/managed by `ElevatorManager` (GameManager child in Facility).

## Spawnable structures (server-spawned via `StructureDistributor` at `StructureSpawnpoint`s)

| Prefab | GOs | Scripts |
|---|---|---|
| `Spawnable Work Station Structure` | 92 | `SpawnableStructure`, `WorkstationController`, `WorkstationAttachmentSelector`, `AttachmentPresetSelector/SummaryEntry/SummaryPrinter`, `ImageBasedRealisticLoadingBar`, `StructurePositionSync`, NetworkIdentity |
| `LargeGunLockerStructure` | 72 | `Locker` + `LockerChamber`×N (Animator×10, AudioSource×10) |
| `RifleRackStructure` | 31 | `Locker`, `LockerChamber` |
| `MiscLocker` | 18 | `Locker`, `LockerChamber`×3 |
| `RegularMedkitStructure` | 7 | `Locker`, `LockerChamber` |
| `AdrenalineMedkitStructure` | 7 | `Locker`, `LockerChamber` |
| `GeneratorStructure` | 44 | `Scp079Generator`, `InteractableCollider`, NetworkIdentity |
| `Scp018/207/244/268/500/1853/2176 PedestalStructure Variant` | 20 | `PedestalScpLocker`, `LockerChamber`, `StructurePositionSync`, NetworkIdentity |

All structures: `StructurePositionSync` + `DynamicCullableBase` + `InteractableCollider`.

## Shooting range targets
`sportTargetPrefab`, `dboyTargetPrefab`, `binaryTargetPrefab` (23–26 GOs): `ShootingTarget`, `InteractableCollider`, NetworkIdentity.

## Admin toys
`PrimitiveObjectToy` (`PrimitiveObjectToy`, NetworkIdentity), `LightSourceToy` (`LightSourceToy`, NetworkIdentity, Light).

## Overcons (SCP-079 overlay icons)
`Door Overcon` (`DoorOvercon`), `Elevator Overcon` (`ElevatorOvercon`), `CCTV Overcon` (`CameraOvercon`), `Tesla Overcon` (`TeslaOvercon`) — rendered by `*OverconRenderer` in SCP-079 HUD.

## Environment props
`Envprp_Ssnl_CM_001…005`, `Envprp_Ssnl_HW_002…014` (no scripts — seasonal clutter spawned by `Clutter`/`ClutterSpawner`), `ParticlesLight`, `AmbientSource` (AudioSource).

---

## UI element prefabs (instantiated into canvases)

| Prefab | Scripts | Used by |
|---|---|---|
| `ServerElement` | `PlayButton` | Server browser row |
| `NewsElement` | `NewsElement`, `ButtonAudio` | Menu news list |
| `CreditsCategory`, `CreditsElement`, `CreditsElement with Role` | `NewCreditsElement` | Credits |
| `PlayerElement` | `PlayerListElement` | Player list (Tab) |
| `PlayerRecord` | `PlayerRecord` | RA player list row |
| `PremadeBanReason` | `CustomSanctionButton` | RA ban window |
| `Status Effect Button` | `RaEffectButton`, `SendButton` | RA effects tab |
| `Door Button` | `RaDoorButton` | RA door panel |
| `LogLine` | TMP text | console |
| `TranslationBrowserElement` | `TranslationBrowserButton` | translation picker |
| `Reflex Sight Config` | `ReflexSightConfigWindow` | sight customization |
| `PlayerID display` | TMP | overhead ID |
| `Spectator Tracker` etc. | see CLAUDE-prefabs-roles.md | spectator UI |

## Mirror debug prefabs (`Assets/Mirror/Components/`)
`GraphCanvas`, `NetworkGraph`, `PingGraph`, `BandwidthGraph`, `FPSMinMaxAvg` — network stats graphs (`ToggleHotkey`, `NetworkPingGraph`, `NetworkBandwidthGraph`, `FpsMinMaxAvgGraph`).

## Resources misc (`Assets/Resources/`)
- `HeadlessBehaviour.prefab` — headless server runtime helper
- `Checkpoint.prefab`, `models/Checkpoint` — checkpoint door model (Animator)
- `models/OH-58D` (87 GOs) — helicopter model; `models/arrow pointer`
- `Rock1_LP … rock19_LP` (27 prefabs) — surface rocks
- `IES cubemap sphere` — light cookie conversion utility (`IESConverter`, Camera×7)

## Third-party example prefabs (ignore for gameplay)
- `Assets/UBER/_Examples/**` (26) — UBER shader showcase
- `Assets/Knife/**`: `DecalsSystem` (`DeferredDecalsSystem` — same script as used by GameManager/Decals), `KnifeMan` (animated test model)
- `Assets/Psychose Interactive/NGSS/**` — shadow library showcase scenes

---

## Misc gameplay prefabs
- `Lunge Death Animation`, ragdolls — see CLAUDE-prefabs-roles.md
- `BloodDecal`, `9mm hole`, `buckshot hole`, `DisruptorHole` — see CLAUDE-prefabs-items.md (Decals)
- `Severed Hands` — SCP-096 victim hands
- `TantrumObj` — SCP-173 tantrum hazard (NetworkIdentity)
- `Amnestic Cloud Hazard` — SCP-939 cloud (NetworkIdentity)
- `Spectator Tracker` — spectator attachment UI

---

## Network-spawnable prefab inventory (registered with Mirror)

Everything carrying `NetworkIdentity` in prefabs: `Player`, all `*Pickup`s, ammo pickups, grenades/projectiles, ragdolls, doors (`* BreakableDoor`), structures (lockers/generators/pedestals/workstation), `ElevatorChamber`(scene-managed), shooting targets, admin toys, `RESPAWN EFFECTS CONTROLLER` (scene), `TantrumObj`, `Amnestic Cloud Hazard`, `DisruptorExplosionSettings`, `LightSourceToy`, `PrimitiveObjectToy`.

---

## Related docs
- `CLAUDE-scenes.md` — where these get spawned/placed in Facility
- `CLAUDE-prefabs-items.md`, `CLAUDE-prefabs-roles.md`, `CLAUDE-firearm.md`
